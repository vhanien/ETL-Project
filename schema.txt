CREATE TABLE covid19 (
  ZIP_CODE VARCHAR PRIMARY KEY,
  NEIGHBORHOOD_NAME VARCHAR,
  BOROUGH_GROUP VARCHAR, 
  COVID_CASE_COUNT VARCHAR, 
  COVID_CASE_RATE VARCHAR, 
  POP_DENOMINATOR VARCHAR,
  COVID_DEATH_COUNT VARCHAR, 
  COVID_DEATH_RATE VARCHAR, 
  PERCENT_POSITIVE VARCHAR, 
  TOTAL_COVID_TESTS VARCHAR
 );
 
 CREATE TABLE zillow (
 ZIP_CODE VARCHAR PRIMARY KEY, 
 JAN_2020 VARCHAR,
 FEB_2020 VARCHAR,
 MAR_2020 VARCHAR,
 APR_2020 VARCHAR,
 MAY_2020 VARCHAR,
 JUN_2020 VARCHAR,
 AVERAGE_2020_YTD VARCHAR
 );
  